apiVersion: kustomize.config.k8s.io/v1beta1 # <-- Kustomization API
kind: Kustomization
resources:
  - namespace.yaml
  - cert.yaml
  - release.yaml
  - passwords.yaml
  - ingress.yaml
configMapGenerator:
  - name: ghost-values
    namespace: ghost
    files:
      - values.yaml=values.yaml
# Do need to learn more about this
configurations:
  - kustomizeconfig.yaml
