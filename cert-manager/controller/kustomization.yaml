apiVersion: kustomize.config.k8s.io/v1beta1 # <-- Kustomization API
kind: Kustomization
# https://kubectl.docs.kubernetes.io/guides/config_management/components/
# apiVersion: kustomize.config.k8s.io/v1alpha1 # <-- Component API
# kind: Component
resources:
  - helm-repo.yaml
  - helm-release.yaml
  # - cluster-issuer.yaml
  # Cert will depend on cluster-issuer and other
  # - cert.yaml
